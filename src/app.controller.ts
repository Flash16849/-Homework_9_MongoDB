import { Controller, Get, Param } from '@nestjs/common';


@Controller()
export class AppController {
  constructor() {}


  
  // @Get()
  // getHello(): string {
  //   return this.appService.getHello();
  // }

  // @Get('/name/:name')
  // getHelloWithName(@Param('name') name: string): string {
  //   return this.appService.getHelloWithName(name);
  // }

  // @Get('/sum/:a/:b/:o')
  // sum(
  //   @Param('a') a: string,
  //   @Param('b') b: string,
  //   @Param('o') o: string,
  // ): number {
  //   return this.appService.sum(parseInt(a), parseInt(b), o);
  // }




}
